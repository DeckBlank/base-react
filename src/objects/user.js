export class User {
    constructor(idCourse, idUnit, idTopic, title, video, map, worksheet) {
        this.idCourse = idCourse;
        this.idUnit = idUnit;
        this.idTopic = idTopic;
        this.title = title;
        this.video = video;
        this.map = map;
        this.worksheet = worksheet;
    }
}